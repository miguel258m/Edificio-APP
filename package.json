{
    "name": "edificio-app-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "backend",
        "frontend-mobile"
    ],
    "scripts": {
        "start": "node backend/server.js",
        "build": "cd frontend-mobile && npm install && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}